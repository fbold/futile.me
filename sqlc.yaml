version: "2"
sql:
  - engine: "postgresql"
    database:
      uri: "${DATABASE_URL}"
    schema: "internal/db/migrations"
    queries: "internal/sqlc/queries.sql"
    gen:
      go:
        package: "sqlc"
        out: "internal/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true